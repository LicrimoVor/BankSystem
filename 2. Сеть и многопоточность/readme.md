# BankSystem 

**BankSystem** — это простая банковская система, реализованная на Rust, с возможностью управления счетами, переводов между аккаунтами и базовыми функциями банковского приложения. 

## Возможности

 - Управление банковскими аккаунтами (создание, удаление, обновление баланса)
 - Переводы между счетами 
 - Проверка и обработка транзакций 

## Установка и запуск  

1. Клонируйте репозиторий:
    ```bash 
    git clone https://github.com/LicrimoVor/BankSystem.git
    cd BankSystem 
    ```
2. Установите зависимости и соберите проект:
    ```bash
    cargo build --release
    ```
    или
    ```bash
    cargo run 
    ```
3. Запустите приложение:
    ```cargo run -p bank --bin utils``` - утилиты
    ```cargo run -p bank --bin demo``` - демо
    ```cargo run -p bank --bin macro``` - тест макросов
    ```cargo run -p parser --bin comparer``` - сравнивает файлы на соответствие транзаций
    ```cargo run -p parser --bin converter``` - конвертирует файл в другой формат

## Тесты
 
Проект покрыт тестами, чтобы убедиться в корректной работе основных функций:
```bash
 cargo test 
```
