[package]
name = "bank"
version = "0.2.1"
edition = "2024"

[dependencies]
macros = { path = "../macros" }
parsers = { path = "../parsers" }
regex = { workspace = true }

[dev-dependencies]
tempfile = "3.23.0"
