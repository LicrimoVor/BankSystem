[package]
name = "bank"
version = "0.7.0"
edition = "2024"

[dependencies]
macros = { path = "../macros" }
regex = { workspace = true }
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
assert_matches = { workspace = true }
tempfile = "3.23.0"
