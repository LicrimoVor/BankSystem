[package]
name = "broken-app"
version = "0.1.0"
edition = "2024"

[profile.release]
debug = true

[dependencies]
fib-rs = "1.2"

[dev-dependencies]
criterion = {version = "0.8", features = ["html_reports"]}

[[bench]]
name = "criterion"
harness = false
